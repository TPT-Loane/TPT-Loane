dist: trusty
language: node_js
node_js:
- node
# latest stable node version  

jobs:
  include:
    - stage: linter
      script:
        - echo "Running linter"
        - "cd fe"
#        - "npm i"
        - "npm run lint"
    - stage: build
      script: skip
    - stage: test
      script: skip

install:
  - "cd fe ; npm install; cd .."

stages:
  - linter
#cache: 
#  - npm
#  - directories:
#    - "fe/node_modules"

